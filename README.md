# Solo Leveling — Minimalist TODO RPG

Минималистичное desktop TODO‑приложение с RPG‑прокачкой.
**Главный прогресс — только от выполнения реальных задач.**
Бои — это испытание навыков и визуализация силы, **EXP за бои запрещён**.

## Ключевые правила

- EXP начисляется **только** за задания/данжи.
- Бои **не дают EXP** и не предназначены для фарма.
- Основная боевая механика — **Tactical Memory**.
- Боссы — **мультифазные**: Tactical Memory + Pressure Puzzle.
- Награды за бои: титулы, косметика, открытие следующих врагов/этажей.
- Боевые попытки зарабатываются **только** выполнением заданий.
- Минимализм важнее количества фич.

## Стек и требования

- Go 1.21+
- Fyne v2 (GUI)
- SQLite (через CGO, `github.com/mattn/go-sqlite3`)
- Ollama (опционально, для AI-рекомендаций)
- Сборка: Windows / macOS / Linux

## Запуск

```bash
cd "путь/к/проекту"

go mod tidy
CGO_ENABLED=1 go run .
```

БД создаётся автоматически: `~/.solo-leveling/game.db`.

## AI-рекомендации (Ollama)

Приложение умеет генерировать персонализированные задания через локальный LLM.
Облачные ключи **не требуются** — всё работает через локальный Ollama.

```bash
ollama pull qwen2.5:7b-instruct
ollama serve

# необязательно, если нужен другой URL/модель
export OLLAMA_BASE_URL="http://localhost:11434"
export OLLAMA_MODEL="qwen2.5:7b-instruct"
```

### Как работает AI-конвейер

1. Пользователь заполняет **AI-профиль** — свободный текст о себе, целях, ограничениях.
2. Ollama получает профиль + текущие статы → генерирует JSON-массив из 8 предложений.
3. Каждое предложение содержит: `title`, `desc`, `minutes`, `effort` (1–5), `friction` (1–3), `stat` (STR/AGI/INT/STA), `tags`.
4. Приложение **само вычисляет ранг** по формуле `score = minutes/10 + effort + friction`, с учётом текущих статов игрока.
5. Результат — до 5 лучших вариантов, отсортированных по слабейшим статам.

### Self-repair

Если LLM возвращает невалидный JSON, клиент автоматически:
1. Пробует 3 стратегии парсинга (прямой, извлечение массива из текста, построчный).
2. Если все провалились — отправляет repair-промпт обратно в LLM.
3. Все запросы и ответы логируются в таблицу `ai_suggestions`.

### Fallback: Rule-based рекомендации

Если Ollama недоступен или AI-профиль пуст, приложение **всегда** работает оффлайн
через rule-based систему рекомендаций на основе **Hunter Profile**.

## Профиль охотника (Hunter Profile)

Структурированный профиль из 11 полей, на основе которого строятся rule-based рекомендации:

| Поле | Описание |
|------|----------|
| О себе | Общая информация |
| Цели | Что хочет достичь |
| Приоритеты | На чём фокус |
| Бюджет времени | Сколько минут в день |
| Физические ограничения | Здоровье, травмы |
| Психологические ограничения | Выгорание, тревожность |
| Распорядок дня | Утро, день, вечер |
| Основные места | Дом, офис, спортзал |
| Нелюбимое | Чего избегать |
| Стиль поддержки | Мягкий / нейтральный / жёсткий |
| Дополнительно | Любые уточнения |

**Как работает rule-based:**
- Пул базовых шаблонов (9 шт.) + пул по целям (здоровье, дисциплина, финансы, навыки, баланс).
- Фильтрация по `maxRank` (определяется из psych constraints) и `maxMinutes` (из time budget).
- Приоритет слабейшим статам.
- Дедупликация с активными заданиями.
- Streak-поддержка: если сегодня нет выполненных заданий, предлагается «Мини-стабильность».

## Система попыток (Battle Attempts)

Бои требуют попыток. Попытки нельзя получить просто так — **только за выполнение заданий**.

| Ранг задания | Попыток |
|-------------|---------|
| E, D | +1 |
| C, B | +2 |
| A, S | +3 |

- Максимум: **8 попыток** одновременно.
- 1 бой (обычный или босс) = 1 попытка.
- Попытки отображаются на вкладке Today и в Башне.

## Башня (Tower System)

Враги организованы в этажную систему — **3 секции по 5 этажей**:

### Секция «Новичок» (этажи 1–5)
| Этаж | Враг | Ранг | Тип | HP | ATK |
|------|------|------|-----|-----|-----|
| 1 | Гоблин | E | regular | 80 | 8 |
| 2 | Скелет-страж | E | regular | 90 | 9 |
| 3 | Волк-тень | D | regular | 120 | 12 |
| 4 | Каменный голем | D | regular | 150 | 14 |
| **5** | **Игрис — Рыцарь Крови** | **C** | **boss** | **300** | **20** |

### Секция «Искатель» (этажи 6–10)
| Этаж | Враг | Ранг | Тип | HP | ATK |
|------|------|------|-----|-----|-----|
| 6 | Тёмный рыцарь | C | regular | 200 | 18 |
| 7 | Ядовитый виверн | C | regular | 220 | 20 |
| 8 | Демон-маг | B | regular | 280 | 24 |
| 9 | Ледяной великан | B | regular | 320 | 26 |
| **10** | **Барука — Король муравьёв** | **A** | **boss** | **500** | **32** |

### Секция «Ветеран» (этажи 11–15)
| Этаж | Враг | Ранг | Тип | HP | ATK |
|------|------|------|-----|-----|-----|
| 11 | Архидемон | A | regular | 450 | 30 |
| 12 | Драконид | A | regular | 500 | 34 |
| 13 | Страж Бездны | S | regular | 600 | 38 |
| 14 | Монарх Хаоса | S | regular | 700 | 42 |
| **15** | **Монарх Теней** | **S** | **boss** | **1000** | **50** |

- Первый враг (Гоблин) разблокирован по умолчанию.
- Победа открывает следующего врага в последовательности.
- Босс на каждом 5-м этаже — мультифазный бой.

## Основной прогресс

- Задания дают EXP в выбранный стат.
- Ранги заданий: E, D, C, B, A, S.
- Уровни растут по формуле:
  `ExpForLevel(level) = 50 + (level-1)*30`
- Выполнение заданий **начисляет боевые попытки**.

### Статы

| Стат | Иконка | Влияние в бою |
|------|--------|---------------|
| STR (Сила) | ⚔️ | Меньше шагов для урона, выше базовый урон |
| AGI (Ловкость) | 🏃 | +1 допустимая ошибка |
| INT (Интеллект) | 📖 | Больше времени показа / короче паттерн |
| STA (Выносливость) | 🛡️ | Больше HP, снижение входящего урона |

## Streak-система и титулы

Непрерывная серия дней с выполненными заданиями:

| Дней подряд | Титул |
|-------------|-------|
| 7 | Начинающий Охотник |
| 30 | Стальная Воля |
| 100 | Легенда Дисциплины |
| 365 | Неостановимый |

- Титулы **сохраняются навсегда** в таблице `streak_titles`.
- Проверка происходит автоматически при завершении задания.

## Бои (Tactical Memory)

- Сетка: 3×3 → 6×6
- Длина паттерна: 4 → 9
- Без повторов подряд, всегда валидный маршрут
- Функция сложности учитывает ранг врага и статы игрока.

**Формулы боя:**
- Базовый урон: `8 + STR*2`
- Необходимые попадания: `patternLength - STR/5`
- Урон врага: `enemy.Attack - STA/5`
- Штраф: `(errors - allowedErrors) * 5` дополнительного урона
- HP игрока: `100 + STA*10`

**Важно:**
- Бои **не дают EXP**.
- Повторные победы не дают прогресса (только история).
- Первая победа даёт титул + бейдж + разблокирует следующего врага.

## Боссы (Multi‑phase)

**Фаза 1:** Tactical Memory (усложнённый)
**Фаза 2:** Pressure Puzzle
- Таймер
- Давление со временем
- Ошибки и попытки

**Связь статов:**
- INT → больше времени
- AGI → 1 прощённая ошибка
- STA → больше попыток
- STR → меньше шагов до успеха

Награды босса:
- уникальный титул (`Босс-победа: <имя>`)
- уникальный бейдж (`Босс: <имя>`)
- открытие следующего врага/этажа

## Данжи

Данжи — серии связанных заданий с требованиями к статам:

- **Каждый данж** содержит набор квестов, которые нужно выполнить.
- **Требования**: минимальные уровни определённых статов для входа.
- **Награды**: бонусный EXP во **все** статы + уникальный титул.
- Статус: locked → available → in_progress → completed.

## Вкладка Today

Главный экран приложения, компактный дашборд:

1. **Карточка персонажа** — имя, уровни всех статов с прогресс-барами.
2. **Streak + Попытки** (объединённая карточка в 2 колонки):
   - Текущий streak, заработанные титулы, прогресс до следующей вехи.
   - Текущие попытки с визуальным баром, таблица попыток по рангам.
3. **Профиль + AI-рекомендации** (объединённая карточка):
   - Кнопки «Профиль охотника», «AI-профиль», «AI-рекомендации».
   - Источник и детали последней генерации.
4. **Задания на сегодня** — список активных заданий с кнопками завершения.

## Архитектура

```
.
├── main.go                             # Точка входа
├── internal/
│   ├── ai/
│   │   └── ollama/
│   │       ├── client.go               # HTTP-клиент Ollama + self-repair
│   │       ├── parse.go                # Парсинг JSON (3 стратегии)
│   │       └── parse_test.go           # Тесты парсинга
│   ├── config/
│   │   └── features.go                 # Feature flags
│   ├── models/
│   │   └── models.go                   # Все модели данных
│   ├── database/
│   │   ├── database.go                 # Подключение к SQLite
│   │   ├── schema.go                   # Миграции (17 таблиц)
│   │   ├── quests.go                   # Квесты, daily, данжи
│   │   ├── stats.go                    # Статы, streak, попытки
│   │   ├── combat.go                   # Враги, бои, награды
│   │   ├── profile.go                  # Hunter Profile CRUD
│   │   └── ai.go                       # AI-профиль + логирование
│   ├── game/
│   │   ├── engine.go                   # Оркестрация, Башня, бои
│   │   ├── quests.go                   # Логика квестов + попытки + streak
│   │   ├── stats.go                    # Статы/статистика
│   │   ├── skills.go                   # Скиллы и мультипликаторы
│   │   ├── dungeons.go                 # Данные данжей
│   │   ├── dungeons_flow.go            # Логика прохождения данжей
│   │   ├── boss.go                     # Интеграция босс‑боёв
│   │   ├── profile.go                  # Rule-based рекомендации (361 строк)
│   │   ├── profile_llm.go             # LLM-интеграция (заглушка)
│   │   ├── ai_recommendations.go       # AI-конвейер: Ollama → ранжирование
│   │   ├── ai_rank_test.go            # Тесты расчёта рангов
│   │   └── combat/
│   │       ├── memory/
│   │       │   ├── memory.go           # Tactical Memory механика
│   │       │   └── memory_test.go      # Тесты
│   │       └── boss/
│   │           └── boss.go             # Босс-механика (2 фазы)
│   └── ui/
│       ├── app.go                      # Главный UI + Башня + бои
│       ├── theme.go                    # Тёмная тема
│       ├── components/
│       │   ├── colors.go               # Палитра цветов
│       │   └── widgets.go              # Общие виджеты
│       └── tabs/
│           ├── context.go              # Shared state для вкладок
│           ├── today.go                # Вкладка Today (дашборд)
│           ├── quests.go               # Вкладка Quests (создание/управление)
│           ├── progress.go             # Вкладка Progress (статистика)
│           └── dungeons.go             # Вкладка Dungeons
```

## Feature Flags

`internal/config/features.go`

```go
type Features struct {
  MinimalMode bool
  Combat      bool
  Events      bool
}
```

Дефолты:
- `MinimalMode = true`
- `Combat = true`
- `Events = false`

В MinimalMode видны только ключевые вкладки: Today / Quests / Progress / Dungeons.

## База данных (17 таблиц)

| Таблица | Назначение |
|---------|-----------|
| `character` | Персонаж (имя, попытки) |
| `hunter_profile` | Профиль охотника (11 полей) |
| `ai_profile` | AI-профиль (свободный текст, singleton) |
| `ai_suggestions` | Лог AI-запросов (raw JSON, модель, ошибки) |
| `stat_levels` | Уровни 4 статов (STR/AGI/INT/STA) |
| `quests` | Задания (активные/завершённые/проваленные) |
| `skills` | Скиллы (заморожены, модель сохранена) |
| `daily_quest_templates` | Шаблоны ежедневных заданий |
| `daily_activity` | Активность по дням (streak) |
| `dungeons` | Данжи |
| `dungeon_quests` | Квесты внутри данжей |
| `completed_dungeons` | Завершённые данжи |
| `enemies` | Враги (15 шт., 3 секции) |
| `streak_titles` | Заработанные streak-титулы |
| `battles` | История боёв |
| `enemy_unlocks` | Разблокированные враги |
| `battle_rewards` | Боевые награды (титулы, бейджи) |

## Что изменено по сравнению с исходной версией

- Введены feature flags и **MinimalMode по умолчанию**.
- Удалена мета‑экономика (ресурсы, гача, крафт, апгрейды).
- Полностью убран EXP из боёв, заменены награды на титулы/бейджи/анлоки.
- Полностью переработана механика боёв на Tactical Memory.
- Добавлены **мультифазные боссы** (Tactical Memory + Pressure Puzzle).
- Рефакторинг проекта по доменам (game/database/ui).

## Последние изменения

- Система **боевых попыток**: бои требуют попыток, которые зарабатываются только заданиями.
- **Башня** (Tower): 15 врагов на 3 этажных секциях (Новичок/Искатель/Ветеран).
- **Streak-титулы**: 4 вехи (7/30/100/365 дней) с постоянными наградами.
- **Hunter Profile**: структурированный профиль (11 полей) для rule-based рекомендаций.
- **AI-рекомендации через Ollama**: локальный LLM генерирует персонализированные задания.
- **Self-repair JSON**: автоматическое исправление сломанного JSON от LLM.
- **Rule-based fallback**: оффлайн-генерация рекомендаций на основе профиля и статов.
- **Компактный Today-дашборд**: объединённые карточки (streak+попытки, профиль+AI).
- Очистка мёртвого кода, удаление экономического цикла.
- Скиллы заморожены: модель остаётся, но UI/переключатели скрыты.

## Тесты

```bash
go test ./...
```

Покрытие:
- `internal/ai/ollama/parse_test.go` — парсинг JSON от LLM (3 стратегии).
- `internal/game/ai_rank_test.go` — расчёт рангов AI-предложений.
- `internal/game/combat/memory/memory_test.go` — генерация паттернов Tactical Memory.
